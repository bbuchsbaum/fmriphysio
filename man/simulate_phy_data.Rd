% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_phy_data}
\alias{simulate_phy_data}
\title{Simulate BOLD fMRI data with known physiological noise}
\usage{
simulate_phy_data(
  n_vox,
  n_time,
  tr = 2,
  nn_frac = 0.2,
  baseline = 100,
  physio_amp_nn = 1.5,
  physio_amp_nt = 0.3,
  neural_amp_nt = 0.8,
  neural_amp_nn = 0.2,
  n_neural = 3L,
  noise_sd = 1,
  seed = NULL
)
}
\arguments{
\item{n_vox}{Number of voxels (at least 20).}

\item{n_time}{Number of time points (at least 20).}

\item{tr}{Repetition time in seconds.}

\item{nn_frac}{Fraction of voxels that are non-neuronal, in (0, 1).}

\item{baseline}{Mean signal level. Each voxel's mean is \code{baseline} scaled
by a random factor near 1; use \code{0} for zero-mean data.}

\item{physio_amp_nn, physio_amp_nt}{Typical absolute loading of the
physiological sources in non-neuronal and neuronal voxels, in units of
\code{noise_sd}, so source-to-noise ratios do not change with \code{noise_sd}.}

\item{neural_amp_nt, neural_amp_nn}{Typical absolute loading of the neural
sources in neuronal and non-neuronal voxels, in units of \code{noise_sd}.}

\item{n_neural}{Number of neural sources.}

\item{noise_sd}{Median white-noise standard deviation.}

\item{seed}{Optional integer seed. The caller's random-number stream is
left untouched when a seed is given.}
}
\value{
A list of class \code{"fmriphysio_sim"} with elements:
\describe{
\item{\code{x}}{Numeric matrix, \code{n_vox} x \code{n_time}: the simulated data.}
\item{\code{nn}}{Logical vector of length \code{n_vox}; \code{TRUE} for non-neuronal
voxels.}
\item{\code{physio}}{Matrix, \code{n_time} x 2: standardized cardiac and
respiratory source time courses.}
\item{\code{neural}}{Matrix, \code{n_time} x \code{n_neural}: standardized neural source
time courses.}
\item{\code{physio_loadings}, \code{neural_loadings}}{Matrices of voxel loadings
(\code{n_vox} x 2 and \code{n_vox} x \code{n_neural}).}
\item{\code{noise_sd}}{Per-voxel white-noise standard deviation.}
\item{\code{voxel_mean}}{Per-voxel baseline.}
\item{\code{physio_hz}}{Named numeric vector of the base cardiac and
respiratory rates in Hz.}
\item{\code{tr}}{The repetition time.}
}
The data satisfy \code{x = voxel_mean + physio_loadings \%*\% t(physio) + neural_loadings \%*\% t(neural) + noise}.
}
\description{
Generates a voxels x time matrix in which a known subset of voxels is
non-neuronal (vasculature / CSF-like) and carries strong physiological
noise, while the remaining (neuronal) voxels carry slow neural signal plus
weaker physiological noise. Because every source and loading is returned,
the data can be used to check how much physiological signal a denoiser
removes and how much neural signal it keeps.
}
\details{
Two physiological sources are simulated in physical units and then sampled
at the repetition time \code{tr}, so they alias exactly as real signals would:
\itemize{
\item a cardiac source whose rate drifts slowly around a base rate drawn from
0.9--1.3 Hz (about 55--80 beats per minute), and
\item a respiratory source whose rate drifts around a base rate drawn from
0.22--0.32 Hz, with slow amplitude modulation.
}

Neural sources are smooth, low-pass (below about 0.08 Hz) random
processes. Each voxel receives white noise whose standard deviation varies
mildly across voxels, and a positive baseline.
}
\examples{
sim <- simulate_phy_data(n_vox = 300, n_time = 120, tr = 2, seed = 1)
dim(sim$x)
table(sim$nn)
}
