% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fast_phy_denoise.R
\name{fast_phy_denoise}
\alias{fast_phy_denoise}
\title{Fast design-free physiological denoising (PHYCAA+-style)}
\usage{
fast_phy_denoise(
  x,
  tr = NULL,
  design = NULL,
  mask = NULL,
  orientation = c("auto", "voxels_by_time", "time_by_voxels"),
  wnn = NULL,
  wnn_method = c("diff_energy"),
  wnn_threshold = c("percentile", "mixture"),
  delta_nt = 0.74,
  delta_nn = 0.95,
  extraction_weight = c("non_neuronal", "neuronal", "none"),
  extractor = c("caa", "dicca", "dipca"),
  lag_order = 1L,
  pca_rank = 20L,
  n_candidates = 30L,
  ratio_thresh = 2,
  pred_thresh = NULL,
  stability = c("none", "half"),
  stability_thresh = 0.3,
  max_passes = 1L,
  return_diagnostics = TRUE,
  svd_engine = c("auto", "svd", "rsvd"),
  use_cpp = TRUE,
  seed = NULL
)
}
\arguments{
\item{x}{Numeric data: a matrix (voxels x time by default; see
\code{orientation}), a 3D/4D array with time as the last dimension, or a
\code{neuroim2::NeuroVec} image (for example a \code{DenseNeuroVec} or
\code{SparseNeuroVec}).}

\item{tr}{Optional repetition time in seconds. Not used by the algorithm;
stored in the result as metadata.}

\item{design}{Optional task design matrix (time points x predictors) used
as a guardrail; see Details.}

\item{mask}{Optional voxel mask: a logical vector, whole-number voxel
indices, a spatial array, or a \code{neuroim2::LogicalNeuroVol}. Voxels
outside the mask are returned unchanged.}

\item{orientation}{Layout of a matrix \code{x}: \code{"voxels_by_time"},
\code{"time_by_voxels"}, or \code{"auto"} (default), which treats a matrix with
fewer rows than columns as time x voxels and says so with a message.}

\item{wnn}{Optional precomputed neuronal-tissue weights in [0, 1]
(1 = neuronal), one per voxel or one per masked voxel. Overrides the
internal computation.}

\item{wnn_method}{Method for computing \code{wNN}; currently only
\code{"diff_energy"}.}

\item{wnn_threshold}{\code{"percentile"} or \code{"mixture"}; passed to
\code{\link[=compute_wNN_diff_energy]{compute_wNN_diff_energy()}} as \code{threshold}.}

\item{delta_nt, delta_nn}{Quantile probabilities in (0, 1) passed to
\code{\link[=compute_wNN_diff_energy]{compute_wNN_diff_energy()}} as \code{nt_q} and \code{nn_q}. Used only when \code{wnn}
is not supplied; \code{delta_nn} is ignored by the mixture threshold.}

\item{extraction_weight}{Per-voxel weighting applied before the low-rank
decomposition: \code{"non_neuronal"} (default; weight \code{1 - wNN}),
\code{"neuronal"} (weight \code{wNN}, as in PHYCAA+), or \code{"none"}. Scoring always
uses the unweighted data.}

\item{extractor}{Component extractor: \code{"caa"} (built in), or \code{"dicca"} /
\code{"dipca"}, which require the 'dipca' package from
\url{https://bbuchsbaum.r-universe.dev}.}

\item{lag_order}{Lag order for DiCCA/DiPCA (whole number >= 1). The CAA
extractor always uses lag 1.}

\item{pca_rank}{Maximum rank of the low-rank subspace (default 20). The
rank used is \verb{min(pca_rank, floor(T / 3), number of analysed voxels)}.}

\item{n_candidates}{Maximum number of candidate components per pass.}

\item{ratio_thresh}{Minimum ratio of median \eqn{R^2} in non-neuronal
voxels to median \eqn{R^2} in neuronal voxels for a candidate to be
selected (strictly greater). The default 2 selects components expressed
more than twice as strongly in non-neuronal as in neuronal tissue.}

\item{pred_thresh}{Optional minimum predictability in [0, 1]: the
canonical lag-1 correlation for \code{"caa"}, or the DiCCA/DiPCA \eqn{R^2}.
\code{NULL} (default) disables the filter.}

\item{stability}{\code{"none"} (default) or \code{"half"}. With \code{"half"}, candidates
are re-extracted independently in each half of the run and a candidate
is kept only if its voxelwise \eqn{R^2} map is reproduced in both halves
(correlation of maps >= \code{stability_thresh}).}

\item{stability_thresh}{Minimum correlation between spatial \eqn{R^2} maps
for split-half stability, in [0, 1].}

\item{max_passes}{Number of extraction/removal passes (whole number >= 1;
1 or 2 recommended).}

\item{return_diagnostics}{Whether to include the \code{diagnostics} element.}

\item{svd_engine}{Low-rank decomposition: \code{"auto"} (exact Gram-matrix
eigendecomposition when the smaller data dimension -- usually the number
of time points -- is at most 2000, randomized SVD otherwise), \code{"svd"}
(exact LAPACK SVD), or \code{"rsvd"} (randomized).}

\item{use_cpp}{Use the compiled scoring kernel (identical results; faster
for large data).}

\item{seed}{Optional seed for the randomized SVD and the DiCCA/DiPCA
extractors. The caller's random number stream is restored afterwards.}
}
\value{
An object of class \code{fast_phy_denoise_result}, a list with
\describe{
\item{\code{x_clean}}{Denoised data of the same type, shape, and
orientation as \code{x} (a \code{NeuroVec} input gives a \code{DenseNeuroVec} or
\code{SparseNeuroVec} in the same space); voxel means are preserved and
excluded voxels are unchanged.}
\item{\code{wNN}}{Neuronal-tissue weight per voxel (1 = neuronal), \code{NA} for
voxels excluded from the analysis.}
\item{\code{regressors}}{Time points x selected components matrix of the
nuisance regressors that were projected out (zero columns if none).}
\item{\code{component_table}}{Data frame with one row per scored candidate:
\code{pass}, \code{candidate}, \code{ratio_nn_nt}, \code{med_nn}, \code{med_nt},
\code{predictability}, \code{stability} (split-half score, \code{NA} unless
\code{stability = "half"}), and \code{selected}.}
\item{\code{active}}{Logical vector, one per voxel: \code{TRUE} for voxels that
were analysed and denoised.}
\item{\code{tr}}{The supplied \code{tr}, or \code{NULL}.}
\item{\code{params}}{The main settings used, including the rank actually
used.}
\item{\code{diagnostics}}{If requested: input and voxel-exclusion summaries,
the full \code{wNN} computation, a per-pass log, and timings.}
}
}
\description{
Removes physiological noise from fMRI time series without a task design,
following the logic of PHYCAA+ with one-shot linear algebra in place of its
repeated searches:
}
\details{
\enumerate{
\item \strong{Tissue weights.} A neuronal-tissue weight \code{wNN} is computed per voxel
from temporal-difference energy (\code{\link[=compute_wNN_diff_energy]{compute_wNN_diff_energy()}}); 1 marks
likely neuronal voxels, 0 likely non-neuronal voxels (vasculature,
cerebrospinal fluid).
\item \strong{Low-rank dynamics.} The voxel-centred data are weighted per voxel --
by default by \code{1 - wNN}, emphasising non-neuronal tissue, where
physiological noise is strongest (see \code{extraction_weight}) -- and reduced
to their leading \code{min(pca_rank, floor(T / 3))} temporal singular vectors.
Temporally predictable components are extracted from that subspace
(\code{\link[=extract_caa_oneshot]{extract_caa_oneshot()}}, or DiCCA/DiPCA via the 'dipca' package).
\item \strong{Selection.} Each candidate is scored by the ratio of its median
voxelwise \eqn{R^2} in non-neuronal voxels to that in neuronal voxels,
computed on the unweighted data. Candidates with ratio above
\code{ratio_thresh} (and optionally predictability above \code{pred_thresh} and
split-half stability) are nuisance regressors.
\item \strong{Removal.} The selected regressors are projected out of every
analysed voxel in one orthogonal projection, and voxel means are
restored. With \code{max_passes > 1}, extraction and selection repeat on the
cleaned data and the union of all selected regressors is projected out
of the original data.
}

Voxels outside \code{mask}, voxels containing non-finite values, and voxels
that are constant over time are excluded from the analysis and returned
unchanged. \code{x_clean} has the type and shape of \code{x}: matrices and arrays
keep their dimensions and orientation, and a \code{neuroim2::NeuroVec} is
returned as a \code{DenseNeuroVec} -- or, for sparse input, a \code{SparseNeuroVec}
with the same mask -- in the same \code{NeuroSpace}. Only voxels inside a
sparse image's mask are analysed.

\strong{Design guardrail.} When \code{design} is supplied, candidate regressors are
orthogonalised against the design (plus an intercept) before scoring and
removal. Denoising therefore cannot change task-effect estimates from a
GLM with that design; the trade-off is that task-correlated physiological
noise is left in place.
}
\section{Departures from PHYCAA+ and limitations}{

PHYCAA+ estimates components from data weighted toward neuronal tissue
(\code{extraction_weight = "neuronal"}) and searches over many subspace ranks.
In simulations with known physiological and neural sources, weighting
toward non-neuronal tissue with a small fixed rank (\code{pca_rank = 20}) and
\code{ratio_thresh = 2} removed substantially more physiological signal while
preserving more neural signal than the PHYCAA+ weighting or larger ranks,
so these are the defaults. Larger ranks let the lag-1 canonical analysis
fit spurious components.

The defaults are better on average, not in every case. When physiological
rhythms alias to nearby frequencies -- to each other, or into the
low-frequency band occupied by neural signal -- they can be mixed within a
candidate whose ratio falls below \code{ratio_thresh}, and part of that noise is
left in the data.

The selection criterion assumes the data contain physiological noise
concentrated in non-neuronal tissue. Without such noise, the
difference-energy weights no longer reflect tissue type (they follow
incidental differences in voxel noise or signal amplitude), and components
carrying neural signal can then be selected and removed. Inspect
\code{component_table} and the removed \code{regressors}, and do not apply the method
to data that have already been physiologically corrected.
}

\examples{
sim <- simulate_phy_data(n_vox = 400, n_time = 150, seed = 1)
fit <- fast_phy_denoise(sim$x, tr = sim$tr)
fit
head(fit$component_table)
}
\references{
Churchill, N. W., & Strother, S. C. (2013). PHYCAA+: An optimized, adaptive
procedure for measuring and controlling physiological noise in BOLD fMRI.
\emph{NeuroImage}, 82, 306--325. \doi{10.1016/j.neuroimage.2013.05.102}

Churchill, N. W., Yourganov, G., Spring, R., Rasmussen, P. M., Lee, W.,
Ween, J. E., & Strother, S. C. (2012). PHYCAA: Data-driven measurement and
removal of physiological noise in BOLD fMRI. \emph{NeuroImage}, 59(2),
1299--1314. \doi{10.1016/j.neuroimage.2011.08.021}
}
\seealso{
\code{\link[=compcor_denoise]{compcor_denoise()}} for a CompCor baseline and
\code{\link[=compute_design_free_qc]{compute_design_free_qc()}} for quality-control summaries.
}
